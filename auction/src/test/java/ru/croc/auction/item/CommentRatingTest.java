package ru.croc.auction.item;

import static org.junit.Assert.*;

import org.junit.Test;

/**
 * @author A.Fedotova
 * @version April 08, 2018
 *
 */
public class CommentRatingTest {

	/**
	 * Test method for
	 * {@link ru.croc.auction.item.Comment.Rating#setRatingNum(ru.croc.auction.user.User)}.
	 */
	@Test
	public void testSetRatingNum() {
		/*
		 * User author = new User.UserBuilder().username("Magician042").build(); //don't
		 * work builder Comment comment = new
		 * Comment.CommentBuilder().from(author).rating(author).build();
		 * 
		 * User viewer1 = new User.UserBuilder().username("BigBoss").build(); User
		 * viewer2 = new User.UserBuilder().username("agent007").build(); User viewer3 =
		 * new User.UserBuilder().username("sherlok<3").build();
		 * 
		 * comment.getRating().setRatingNum(viewer1).setRatingNum(viewer2).setRatingNum(
		 * viewer3).setRatingNum(viewer2);
		 * 
		 * assertEquals(comment.getRating().getRatingNum(), 4);
		 */
		fail("Not yet implemented");
	}

}
